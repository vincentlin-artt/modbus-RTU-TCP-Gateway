#pragma once
// ================================================================
//  modbus_rtu.h  –  Modbus RTU master over RS485 (protocol layer)
//  Prototypes live in config.h (mbRtuBegin / mbRtuReadPoint / mbRtuWritePoint).
//  This header just documents the module and pulls in the shared types.
// ================================================================
#include "config.h"
